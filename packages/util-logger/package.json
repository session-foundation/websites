{
  "name": "@session/util-logger",
  "version": "0.0.0",
  "private": true,
  "exports": "./index.ts",
  "scripts": {
    "check-types": "tsc --noEmit",
    "lint": "biome lint",
    "lint:fix": "biome lint --write",
    "format": "biome format --write",
    "check": "biome check",
    "check:fix": "biome check --write",
    "test": "jest"
  },
  "devDependencies": {
    "@session/testing": "workspace:*",
    "@session/typescript-config": "workspace:*"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10",
    "yarn": "use pnpm",
    "npm": "use pnpm"
  },
  "dependencies": {
    "pino": "^9.2.0",
    "pino-pretty": "^11.2.1",
    "@session/logger": "workspace:*",
    "@session/util-js": "workspace:*"
  }
}
